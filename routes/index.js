var express = require('express');
var router = express.Router();

/* GET home page. */
// router.get('/', function(req, res, next) {
//   res.render('index', { title: 'Bolnore Coffee Shop' });
// });
// 
//  Simply allow the static access to the public index.html file

module.exports = router;
